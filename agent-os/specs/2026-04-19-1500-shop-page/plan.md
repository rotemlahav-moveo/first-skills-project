# Shop page — Plan

## Tasks completed

1. **Spec documentation** — This folder (`plan.md`, `shape.md`, `standards.md`, `references.md`, `visuals/README.md`).
2. **Cart provider** — `CartProvider` + `useCart()`; `CartPage` refactored; provider wraps app in `main.tsx`.
3. **Mock catalog** — `shop/types.ts`, `shop/mockProducts.ts`, `shop/filterProducts.ts` with client-side filters + sort.
4. **Shop route & UI** — `ShopPage`, sections, components matching Figma Make shop layout; add-to-cart via `useCart`.
5. **Homepage** — `HowItWorksSection` “View All” → `Link` to `/shop`.
6. **QA** — Nx lint for `front`.

## Figma

- [Figma Make — E-commerce Website Wireframe (shop)](https://www.figma.com/make/fRvRx3rPlaAGZc9I7dRRhM/E-commerce-Website-Wireframe?preview-route=%2Fshop)
