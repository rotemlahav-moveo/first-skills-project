# Visual References

No static image files were provided during shaping.

Use the connected Figma project and select the best-matching cart frame as the UI reference for:
- layout hierarchy
- spacing scale
- typography scale
- breakpoint behavior

If screenshots are captured later, store them in this folder.
