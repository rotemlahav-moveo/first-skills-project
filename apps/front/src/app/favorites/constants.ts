export { FAVORITES_MAX_ITEMS } from '@shared/favorites-contracts';

/** Legacy cookie name so existing browsers keep their saved list. */
export const FAVORITES_COOKIE_NAME = 'snapstyle_wishlist_v1';

/** One year, seconds */
export const FAVORITES_COOKIE_MAX_AGE = 60 * 60 * 24 * 365;
