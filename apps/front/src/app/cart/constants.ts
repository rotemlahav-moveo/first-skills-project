export { CART_MAX_LINE_ITEMS } from '@shared/cart-contracts';

/** Cookie name for guest + sync with server cart when logged in. */
export const CART_COOKIE_NAME = 'snapstyle_cart_v2';

/** One year, seconds */
export const CART_COOKIE_MAX_AGE = 60 * 60 * 24 * 365;
